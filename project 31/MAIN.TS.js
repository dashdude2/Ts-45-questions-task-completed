var users = ["umer", "rohit", "mila", "admin"];
if (users.length === 0) {
    console.log("We need to find some users!");
}
else
    [];
{
    console.log("ALL USER AS BEEN REMOVED " + users.length);
}
